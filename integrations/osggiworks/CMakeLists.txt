IF(WIN32)
    SET(LIB_POSTFIX "_x86.lib")
    IF(CMAKE_CL_64)
        SET(LIB_POSTFIX "_x64.lib")
    ENDIF(CMAKE_CL_64)
    
    FIND_PATH(NVGIWORKS_INCLUDE_DIR GI_Interface.h)
    FIND_LIBRARY(NVGIWORKS_LIBRARY GFSDK_VXGI${LIB_POSTFIX})

    IF(WIN32 AND NVGIWORKS_INCLUDE_DIR AND NVGIWORKS_LIBRARY)
        SET(EXTERNAL_INCLUDE_DIR ${NVGIWORKS_INCLUDE_DIR} ${OPENGL_INCLUDE_DIR})
        SET(EXTERNAL_LIBRARIES ${NVGIWORKS_LIBRARY} ${OPENGL_LIBRARIES})
        
        SET(EXAMPLE_NAME osggiworks)
        SET(EXAMPLE_FILES osggiworks.cpp)
        START_EXAMPLE()
    ENDIF(WIN32 AND NVGIWORKS_INCLUDE_DIR AND NVGIWORKS_LIBRARY)
ENDIF()
