FIND_PATH(INTEL_PCSDK_INCLUDE_DIR pxcbase.h)
FIND_LIBRARY(INTEL_PCSDK_LIBRARY libpxc)

IF(INTEL_PCSDK_INCLUDE_DIR AND INTEL_PCSDK_LIBRARY)
    SET(EXTERNAL_INCLUDE_DIR ${INTEL_PCSDK_INCLUDE_DIR})
    SET(EXTERNAL_LIBRARIES ${INTEL_PCSDK_LIBRARY})
    
    SET(EXAMPLE_NAME osgperceptual)
    SET(EXAMPLE_FILES osgperceptual.cpp)
    START_EXAMPLE()
ENDIF(INTEL_PCSDK_INCLUDE_DIR AND INTEL_PCSDK_LIBRARY)
