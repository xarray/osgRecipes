FIND_PATH(MSKINECT_INCLUDE_DIR NuiApi.h)
FIND_LIBRARY(MSKINECT_LIBRARY NAMES Kinect Kinect10)

IF(MSKINECT_INCLUDE_DIR AND MSKINECT_LIBRARY)
    SET(EXTERNAL_INCLUDE_DIR ${MSKINECT_INCLUDE_DIR})
    SET(EXTERNAL_LIBRARIES ${MSKINECT_LIBRARY})
    
    SET(EXAMPLE_NAME osgmskinect)
    SET(EXAMPLE_FILES osgmskinect.cpp KinectHandler.cpp)
    START_EXAMPLE()
ENDIF(MSKINECT_INCLUDE_DIR AND MSKINECT_LIBRARY)
