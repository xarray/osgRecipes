FIND_PATH(LIBCEF_INCLUDE_DIR include/cef_app.h)
FIND_LIBRARY(LIBCEF_LIBRARY libcef)

IF(WIN32 AND LIBCEF_INCLUDE_DIR AND LIBCEF_LIBRARY)
    SET(EXTERNAL_INCLUDE_DIR ${LIBCEF_INCLUDE_DIR})
    SET(EXTERNAL_LIBRARIES ${LIBCEF_LIBRARY})
    
    SET(EXAMPLE_NAME osgcef)
    SET(EXAMPLE_FILES osgcef.cpp)
    START_EXAMPLE()
ENDIF(WIN32 AND LIBCEF_INCLUDE_DIR AND LIBCEF_LIBRARY)
