SET(EXAMPLE_NAME osgnanovg)
SET(EXAMPLE_FILES
    osgnanovg.cpp
    nanovg/nanovg.c
)

SET(EXTERNAL_INCLUDE_DIR ${GLEW_INCLUDE_DIR} ${OPENGL_INCLUDE_DIR})
SET(EXTERNAL_LIBRARIES ${GLEW_LIBRARY} ${OPENGL_LIBRARIES})
ADD_DEFINITIONS(-DGLEW_STATIC -DNANOVG_GLEW -DNANOVG_GL2_IMPLEMENTATION)
START_EXAMPLE()
