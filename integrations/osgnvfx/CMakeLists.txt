FIND_PATH(NVFX_INCLUDE_DIR INVFX.h)
FIND_LIBRARY(NVFX_LIBRARY FxLib)
FIND_LIBRARY(NVFX_OPENGL_LIBRARY FxLibGL)
FIND_LIBRARY(NVFX_PARSER_LIBRARY FxParser)

IF(NVFX_INCLUDE_DIR AND NVFX_LIBRARY AND GLEW_LIBRARY)
    SET(EXTERNAL_INCLUDE_DIR ${NVFX_INCLUDE_DIR} ${OPENGL_INCLUDE_DIR})
    SET(EXTERNAL_LIBRARIES ${OPENGL_LIBRARIES} ${GLEW_LIBRARY}
        ${NVFX_LIBRARY} ${NVFX_OPENGL_LIBRARY} ${NVFX_PARSER_LIBRARY})
    
    SET(EXAMPLE_NAME osgnvfx)
    SET(EXAMPLE_FILES osgnvfx.cpp)
    START_EXAMPLE()
ENDIF(NVFX_INCLUDE_DIR AND NVFX_LIBRARY AND GLEW_LIBRARY)
