ADD_DEFINITIONS(-DOTL_ODBC)
SET(LIBRARY_NAME osgdb_otl)
SET(LIBRARY_FILES ReaderWriterOTL.cpp)

IF(WIN32)
    SET(EXTERNAL_LIBRARIES odbc32.lib)
ELSE(WIN32)
    ADD_DEFINITIONS(-DOTL_ODBC_UNIX)
ENDIF(WIN32)
START_LIBRARY()

SET(EXAMPLE_NAME osgotl)
SET(EXAMPLE_FILES osgotl.cpp)
START_EXAMPLE()
