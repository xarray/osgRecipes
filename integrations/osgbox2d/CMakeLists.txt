FIND_PATH(BOX2D_INCLUDE_DIR Box2D/Box2D.h)
FIND_LIBRARY(BOX2D_LIBRARY Box2D)

IF(BOX2D_INCLUDE_DIR AND BOX2D_LIBRARY)
    SET(EXTERNAL_INCLUDE_DIR ${BOX2D_INCLUDE_DIR})
    SET(EXTERNAL_LIBRARIES ${BOX2D_LIBRARY})
    
    SET(EXAMPLE_NAME osgbox2d)
    SET(EXAMPLE_FILES osgbox2d.cpp)
    START_EXAMPLE()
ENDIF(BOX2D_INCLUDE_DIR AND BOX2D_LIBRARY)
