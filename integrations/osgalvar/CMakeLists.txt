IF(OpenCV_FOUND AND ALVAR_FOUND)
    SET(EXTERNAL_INCLUDE_DIR ${OpenCV_INCLUDE_DIRS} ${ALVAR_INCLUDE_DIRS})
    SET(EXTERNAL_LIBRARIES ${OpenCV_LIBRARIES} ${ALVAR_LIBRARIES})
    
    SET(EXAMPLE_NAME osgalvar)
    SET(EXAMPLE_FILES osgalvar.cpp)
    START_EXAMPLE()
ENDIF(OpenCV_FOUND AND ALVAR_FOUND)
