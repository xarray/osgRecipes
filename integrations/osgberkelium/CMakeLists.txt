FIND_PATH(BERKELIUM_INCLUDE_DIR berkelium/Berkelium.hpp)
FIND_LIBRARY(BERKELIUM_LIBRARY berkelium)

IF(BERKELIUM_INCLUDE_DIR AND BERKELIUM_LIBRARY)
    SET(EXTERNAL_INCLUDE_DIR ${BERKELIUM_INCLUDE_DIR})
    SET(EXTERNAL_LIBRARIES ${BERKELIUM_LIBRARY})
    
    SET(EXAMPLE_NAME osgberkelium)
    SET(EXAMPLE_FILES osgberkelium.cpp)
    START_EXAMPLE()
ENDIF(BERKELIUM_INCLUDE_DIR AND BERKELIUM_LIBRARY)
