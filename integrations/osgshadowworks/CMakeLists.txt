IF(WIN32)
    SET(LIB_POSTFIX "win32.lib")
    IF(CMAKE_CL_64)
        SET(LIB_POSTFIX "win64.lib")
    ENDIF(CMAKE_CL_64)
    
    FIND_PATH(NVSSAO_INCLUDE_DIR GFSDK_SSAO.h)
    FIND_LIBRARY(NVSSAO_LIBRARY GFSDK_SSAO.${LIB_POSTFIX})

    IF(WIN32 AND NVSSAO_INCLUDE_DIR AND NVSSAO_LIBRARY)
        SET(EXTERNAL_INCLUDE_DIR ${NVSSAO_INCLUDE_DIR} ${OPENGL_INCLUDE_DIR})
        SET(EXTERNAL_LIBRARIES ${NVSSAO_LIBRARY} ${OPENGL_LIBRARIES})
        
        SET(EXAMPLE_NAME osgshadowworks)
        SET(EXAMPLE_FILES osgshadowworks.cpp)
        START_EXAMPLE()
    ENDIF(WIN32 AND NVSSAO_INCLUDE_DIR AND NVSSAO_LIBRARY)
ENDIF()
