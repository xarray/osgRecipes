FIND_PATH(GIZMO_INCLUDE_DIR IGizmo.h)
FIND_LIBRARY(GIZMO_LIBRARY libgizmo)

IF(GIZMO_INCLUDE_DIR AND GIZMO_LIBRARY)
    SET(EXTERNAL_INCLUDE_DIR ${GIZMO_INCLUDE_DIR} ${OPENGL_INCLUDE_DIR})
    SET(EXTERNAL_LIBRARIES ${GIZMO_LIBRARY} ${OPENGL_LIBRARIES})
    
    SET(EXAMPLE_NAME osggizmo)
    SET(EXAMPLE_FILES osggizmo.cpp)
    START_EXAMPLE()
ENDIF(GIZMO_INCLUDE_DIR AND GIZMO_LIBRARY)
